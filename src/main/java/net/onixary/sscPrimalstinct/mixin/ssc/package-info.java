/**
 * 注入 SSC 内部类的 Mixin（配置：resources/ssc-primalstinct-ssc.mixins.json）。
 * 每个注入点必须先登记 docs/MIXIN_INVENTORY.md（目标/目的/remap/受影响行为/回归用例）。
 * SSC 自身类名与成员名在发布产物中不变；签名中的 Minecraft 类型经 refmap 重映射。
 */
package net.onixary.sscPrimalstinct.mixin.ssc;
